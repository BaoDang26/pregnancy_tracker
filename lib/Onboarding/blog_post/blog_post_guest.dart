import 'package:flutter/material.dart';

class BlogPostGuest extends StatelessWidget {
  const BlogPostGuest({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(
          children: [
            Text('Blog Post Guest'),
          ],
        ),
      ),
    );
  }
}
